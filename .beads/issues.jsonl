{"id":"cm-0jy","title":"Gate: full check after Wave 2 (cm-8k0s)","description":"Final full-verification gate after cm-8k0s. Build + test + /code-review on this sprint's commits.","notes":"## prepared.prompt\n\nRun these in order. Fix build/test failures inline. For /code-review findings, fix-or-file per gg-execute rules.\n\n1. cd tui \u0026\u0026 go build -o conductor-tui . \u0026\u0026 go vet ./... \u0026\u0026 go test ./...\n2. python -m py_compile server.py\n3. bash -n reference/*.sh\n4. /code-review HEAD~N..HEAD  (N = commits in this sprint — count from cm-y7t's first commit)\n\nClose only when all pass.","status":"open","priority":1,"issue_type":"chore","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-23T17:52:59Z","created_by":"GGPrompts","updated_at":"2026-04-23T17:53:00Z","labels":["gate","gate:full","ready"],"dependencies":[{"issue_id":"cm-0jy","depends_on_id":"cm-8k0s","type":"blocks","created_at":"2026-04-23T13:53:02Z","created_by":"GGPrompts","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"cm-5cl","title":"Gate: standard check after Wave 1 (cm-y7t)","description":"Standard verification gate after cm-y7t ships. Build + vet + test TUI, syntax-check python + shell.","notes":"## prepared.prompt\n\nRun these. Fix any failure inline. Pre-existing warnings count. Re-run to confirm clean.\n\n1. cd tui \u0026\u0026 go build -o conductor-tui . \u0026\u0026 go vet ./... \u0026\u0026 go test ./...\n2. python -m py_compile server.py\n3. bash -n reference/audio-announcer.sh reference/state-tracker.sh reference/tmux-status-claude.sh\n\nClose the gate only when everything passes.","status":"closed","priority":1,"issue_type":"chore","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-23T17:52:58Z","created_by":"GGPrompts","updated_at":"2026-04-23T18:03:44Z","closed_at":"2026-04-23T18:03:44Z","close_reason":"Wave 1 clean: go build/vet/test, python syntax, bash syntax all pass","labels":["gate","gate:standard","ready"],"dependencies":[{"issue_id":"cm-5cl","depends_on_id":"cm-y7t","type":"blocks","created_at":"2026-04-23T13:53:01Z","created_by":"GGPrompts","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"cm-y7t","title":"Unify TTS config: audio on/off, volume, bridge state-tracker to canonical config","description":"Two audio systems have drifted apart. Unify them through the canonical config and expose on/off + volume in the TUI Settings panel.\n\n## Current state\n\n| Audio surface | Rate source | Voice source | Volume source | On/off |\n|---|---|---|---|---|\n| MCP `speak()` tool (server.py:520, 531) | `~/.config/conductor/config.json` `voice.rate` | `voice.default` or per-worker assignment | **not threaded** (no `--volume` arg) | **none — always on** |\n| CC state-change chimes (reference/audio-announcer.sh:57, 154) | env `CLAUDE_RATE` from `~/.claude/settings.json` | env `CLAUDE_VOICE` | env `CLAUDE_VOLUME` | env `CLAUDE_AUDIO` (0/1) |\n| TUI Voice preview (tui/settings.go testVoice) | `voice.rate` via helper | `voice.default` | not threaded | n/a |\n\nReported drift: TUI shows voice.rate = `+15%` while `CLAUDE_RATE = +20%` — the same user, different audio paths, different values, no way to see both in one place.\n\n## Proposed end state\n\nCanonical config grows:\n```json\n\"voice\": {\n  \"default\": \"en-GB-SoniaNeural\",\n  \"rate\": \"+15%\",\n  \"pitch\": \"+0Hz\",\n  \"volume\": \"+0%\",\n  \"random_per_worker\": true,\n  \"enabled\": true\n}\n```\n\n### Bridge state-tracker to canonical config\n- `reference/audio-announcer.sh` (lines 49–59): read voice/rate/pitch/volume from `~/.config/conductor/config.json` via `jq`, falling back to the existing `CLAUDE_VOICE`/`CLAUDE_RATE`/`CLAUDE_VOLUME` env vars only if the config is missing a field. Same pattern used by the audio-section jq bridge noted in CLAUDE.md.\n- `reference/state-tracker.sh` (lines 101, 118, 140): check `voice.enabled` in the canonical config first; keep `CLAUDE_AUDIO` as fallback for backwards-compat.\n\n### Thread volume + pitch through MCP speak()\n- `server.py speak()` around line 530: pass `--volume` and `--pitch` to edge-tts, reading from `voice.volume` / `voice.pitch` defaults.\n- `speak()` short-circuits with a descriptive return when `voice.enabled == false` (no generation, no playback).\n\n### Expose in TUI Settings — Voice section\nAdd rows above the voice pool, alongside the rate/pitch editors from cm-06j:\n- **Enabled**: toggle row (space flips). Writes `voice.enabled`.\n- **Volume**: stepper `+XX%` (left/right adjusts ±5%, same UX as rate/pitch). Writes `voice.volume`.\n\nUpdate `voicePreambleLines` / `voiceCursorLine` helpers in tui/update_keyboard.go to account for the two new rows — same pattern cm-cjk and cm-06j already established.\n\nExtend `tui/settings_voice_test.go TestVoiceCursorLineMatchesContent` to cover the new cursor range.\n\n## Key files\n- server.py (speak tool, ~line 485–545)\n- reference/audio-announcer.sh (RATE/VOICE/VOLUME reads, ~49–60)\n- reference/state-tracker.sh (CLAUDE_AUDIO guards, ~101/118/140)\n- tui/settings.go (add getters/setters for volume + enabled; thread volume into testVoice)\n- tui/model.go (updateSettingsContent voice section — add enabled toggle + volume stepper rows)\n- tui/update_keyboard.go (handleSettingsKeys voice cursor: insert enabled + volume into ordering, update voicePoolStart constant)\n- tui/settings_voice_test.go (extend cursor-line test)\n\n## Verify\n1. Set `voice.enabled = false` in config → MCP `speak(\"hi\")` returns \"audio disabled\" without calling edge-tts; state-tracker.sh chimes are silent.\n2. Set `voice.volume = \"-20%\"` in TUI → MCP speak and state-tracker.sh chimes both play at -20%.\n3. Confirm legacy env vars still win when canonical config key is missing (backwards compat for users who haven't migrated).\n\n## Non-goals\n- Not touching `CLAUDE_AUDIO` / `CLAUDE_RATE` env vars — they remain as fallback.\n- Not adding per-worker volume or per-worker enabled (use case hasn't come up).\n\n## When done\nbd close \u003cid\u003e --reason \"audio on/off + volume in TUI, state-tracker bridged to canonical config\"","notes":"## prepared.prompt\n\n## Context\nTwo audio systems drifted apart (voice.rate = +15% in canonical config vs CLAUDE_RATE = +20% in ~/.claude/settings.json). User reported the confusion directly. Description holds the full spec — read it first.\n\n## Task\n\n1. Extend canonical config schema: `voice.volume` (default `\"+0%\"`) and `voice.enabled` (default `true`).\n2. Thread volume + pitch through `server.py speak()` (line ~530 — add `--volume` and `--pitch` to the edge-tts argv). Early-return with a descriptive string when `voice.enabled` is false (no generation, no playback).\n3. Bridge `reference/audio-announcer.sh` (lines 49-60) and `reference/state-tracker.sh` (CLAUDE_AUDIO guards at lines 101/118/140): read voice/rate/pitch/volume/enabled from `~/.config/conductor/config.json` via jq, fall back to `CLAUDE_*` env vars only when the config key is missing. Use the same pattern as the existing audio-section jq bridge noted in CLAUDE.md.\n4. TUI Voice section: add a toggle row for `voice.enabled` (space flips) and a stepper row for `voice.volume` (left/right ±5%, mirror rate/pitch). Update `voicePreambleLines` / `voiceCursorLine` in tui/update_keyboard.go and the `voicePoolStart` constant. Extend `tui/settings_voice_test.go TestVoiceCursorLineMatchesContent` to cover the new cursor range.\n\n## Key Files\n- server.py (speak tool, ~485-545)\n- reference/audio-announcer.sh (RATE/VOICE/VOLUME reads, lines 49-60)\n- reference/state-tracker.sh (CLAUDE_AUDIO guards, lines 101/118/140)\n- tui/settings.go (add getters/setters; thread volume into testVoice)\n- tui/model.go (updateSettingsContent voice section)\n- tui/update_keyboard.go (handleSettingsKeys voice cursor, voicePoolStart const)\n- tui/settings_voice_test.go\n\n## docs:update\nUpdate root CLAUDE.md with ONLY:\n- Canonical config is the single source of truth for voice/audio settings (env CLAUDE_* vars are fallback only)\n- voice.enabled gates all audio paths (MCP speak + state-tracker chimes)\nNo file paths, no build commands.\n\n## Verify\n1. cd tui \u0026\u0026 go build -o conductor-tui . \u0026\u0026 go vet ./... \u0026\u0026 go test ./...\n2. python -m py_compile server.py\n3. Set voice.enabled=false in config → MCP speak('hi') short-circuits; state-tracker chimes silent.\n4. Set voice.volume='-20%' → MCP speak + state-tracker both play at -20%.\n5. Unset voice.rate in config → audio-announcer.sh falls back to CLAUDE_RATE env var.\n\n## When Done\nbd close cm-y7t --reason \"TTS config unified, audio on/off + volume in TUI, state-tracker bridged\"\n## Retro\n- What worked: Reusing parseSignedNumber/formatSigned for volume stepper kept the TUI code tight. Factoring voice fixed-row indices into named constants (voiceCursorEnabled/Rate/Pitch/Volume/Random) made inserting two new rows a surgical edit with no drift between renderer, handlers, and cursor math.\n- What was unclear: jq's // operator silently treats the literal JSON false as absent, which made the first state-tracker bridge accept CLAUDE_AUDIO=1 even when voice.enabled=false. Caught by direct case testing. Fixed with an explicit `has(\"enabled\")` presence probe before reading the value.","status":"closed","priority":1,"issue_type":"feature","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-23T17:49:27Z","created_by":"GGPrompts","updated_at":"2026-04-23T18:03:08Z","closed_at":"2026-04-23T18:03:08Z","close_reason":"TTS config unified, audio on/off + volume in TUI, state-tracker bridged","labels":["audio","docs:update","follow-up","ready","settings","tui"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"cm-gq2","title":"Gate: full verification after Settings migration (cm-3gw follow-ups)","description":"Final gate after cm-cjk + cm-06j + cm-b6r. Build TUI, run tests, smoke-test Settings end-to-end, confirm canonical config round-trips, run /code-review, and verify root CLAUDE.md reflects Profile CRUD / Timing editing per cm-b6r docs:update. Fix anything that breaks — don't just report.","notes":"## prepared.prompt\n\n## Context\nFinal gate after cm-cjk (scroll/header fixes) + cm-06j (Voice editing) + cm-b6r (Profiles CRUD + Timing editor) complete the Settings migration started by cm-3gw.\n\n## Task\nVerify the full Settings tab works end-to-end. Do not just report — fix in place if anything breaks, re-run, close only when clean.\n\n1. Build: `cd tui \u0026\u0026 go build -o conductor-tui .` — must succeed, no warnings.\n2. Tests: `cd tui \u0026\u0026 go test ./...` and `pytest` at repo root if present — must pass.\n3. Smoke test: launch conductor-tui, cycle panel 1 to Settings. In each sub-section (Voice, Profiles, Timing), verify:\n   - Scrolling works when content exceeds panel height.\n   - Editing any field persists to ~/.config/conductor/config.json (confirm with jq).\n   - Random-per-worker toggle, reset-assignments, add/delete profile all round-trip.\n4. Run /code-review on the three commits (cm-cjk, cm-06j, cm-b6r). Address high-confidence findings.\n5. Confirm root CLAUDE.md reflects Profile CRUD / Timing editing per cm-b6r's docs:update.\n\n## When Done\nbd close cm-gq2 --reason \"settings migration fully verified\"","status":"closed","priority":1,"issue_type":"chore","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-23T16:58:35Z","created_by":"GGPrompts","updated_at":"2026-04-23T17:32:35Z","closed_at":"2026-04-23T17:32:35Z","close_reason":"settings migration fully verified: build+vet+test clean, /code-review applied 11 fixes inline","labels":["ready"],"dependencies":[{"issue_id":"cm-gq2","depends_on_id":"cm-b6r","type":"blocks","created_at":"2026-04-23T12:58:35Z","created_by":"GGPrompts","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"cm-cjk","title":"TUI Settings panel: scroll broken + header gets hidden","description":"Two user-visible defects in the Settings tab introduced by cm-3gw:\n\n## Bug 1 — scroll offset never advances with cursor\n\n`handleSettingsKeys` in `tui/update_keyboard.go:377-396` only increments/decrements `m.settingsCursor` on up/down/j/k. It never touches `m.sessionsScrollOffset`. The view layer (`tui/view.go:598-602`) does respect `sessionsScrollOffset` for top-tab panels including settings, but nothing ever changes it while in Settings.\n\nEffect:\n- Voice section has 14 entries in `voicePool` plus ~9 preamble lines (HEADER, tabs row, divider, section header, blank rows). On a panel tall enough for ~15 content lines, cursor positions 7+ fall off the bottom.\n- Profiles/Timing sections: up/down are explicit no-ops (see lines 385-386, 395-396), so those tabs can't scroll at all even though content may exceed the panel.\n\nRepro:\n1. Launch conductor-tui, focus top panel, press `1` twice to reach Settings tab.\n2. Voice section. Press down ~10 times. Cursor indicator (►) disappears from view; no scrollOffset update.\n\n## Bug 2 — TUI header text hidden when Settings tab active\n\nUser-reported: switching to Settings shifts/hides the TUI header row (top title or Sessions|Templates|Settings tab bar). Not yet isolated — could be:\n- `HEADER:Settings` being the first content line (`model.go:753`) rendering under `tableHeaderStyle` in a way that overlaps the outer border title.\n- A line-height miscount pushing content up past the panel frame.\n\nNeeds repro + investigation with a screenshot or width reproduction.\n\n## Fix sketch\n\n- Auto-scroll on cursor move: when `settingsCursor` advances past `sessionsScrollOffset + availableContentLines`, bump the offset. When it retreats past `sessionsScrollOffset`, decrement. Account for the preamble row count (~9 rows before first voice entry).\n- Allow up/down (and PgUp/PgDn) to drive `sessionsScrollOffset` directly in Profiles/Timing sections where there is no cursor.\n- Header: reproduce in a narrow terminal; likely a styling interaction between `HEADER:` content and the panel's own title bar. Consider dropping the redundant `HEADER:Settings` line — the panel's top border already shows the active tab.\n\n## Context\n\ncm-3gw migrated Settings from MCP tools into the TUI. Voice picker works; Profiles/Timing are stubs. cm-b6r tracks fuller CRUD for those sections.","notes":"## Retro\n- What worked: Bug 2 confirmed by static analysis without launching TUI — panel tab bar is rendered in the top border (view.go:721 `customTopBorder`), so the inner `HEADER:Settings` line was pure duplication and its removal cannot hide the tab bar. Bug 1 fix mirrors the existing `adjustSessionsScrollToSelection` pattern (update_keyboard.go:1184), so the scroll math is consistent with the rest of the panel.\n- What was unclear: The task said \"~9 preamble lines\"; actual count (after dropping HEADER+blank) is 7. Documented as `voicePreambleLines = 7` with a layout comment in update_keyboard.go so future additions to the Voice preamble will be caught by grep.\n- Caveat: Did not interactively verify the UX — only `go build` + `go vet` + code review of the scroll-math logic, per orchestrator rules. PgUp/PgDn in Voice section also drags the cursor by one viewport for predictability; Profiles/Timing scroll directly without a cursor. Section switch resets `sessionsScrollOffset` so a previously-scrolled Voice view does not leak into Profiles/Timing.\n- Follow-ups unblocked: cm-06j (Voice completeness) and cm-b6r (Profiles/Timing editors) can now add rows without the cursor scrolling out of view.","status":"closed","priority":1,"issue_type":"bug","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-23T16:44:57Z","created_by":"GGPrompts","updated_at":"2026-04-23T17:06:22Z","closed_at":"2026-04-23T17:06:22Z","close_reason":"scroll tracking + header shift fixed in Settings tab","labels":["bug","ready","tui"],"dependencies":[{"issue_id":"cm-cjk","depends_on_id":"cm-3gw","type":"blocks","created_at":"2026-04-23T12:44:57Z","created_by":"GGPrompts","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"id":"cm-9vs","title":"Remove plugin hooks duplication — unhook plugin state-tracker.sh","description":"## Problem\n\nAfter installing the `conductor@conductor-mcp` plugin, every Claude Code event fires both:\n- `~/.claude/hooks/scripts/state-tracker.sh` (user-level, 509 lines, customized, used by other apps)\n- `${CLAUDE_PLUGIN_ROOT}/hooks/scripts/state-tracker.sh` (plugin-bundled, 286-line baseline)\n\nThey both write `/tmp/claude-code-state/*.json` — racy, and the plugin version is a stale subset of the user version. `/reload-plugins` jumped hook count from 4 → 12.\n\n## Fix\n\nEmpty out `plugins/conductor/hooks/hooks.json` (or remove the `hooks/` directory from the plugin entirely). Keep the plugin's `state-tracker.sh` file available as a reference baseline but do not register it as a hook.\n\n## Rationale\n\nThe user-level `~/.claude/hooks/scripts/state-tracker.sh` is authoritative across multiple projects — it has subagent counting, context linking, and other customizations. The plugin should not claim ownership of this cross-cutting infrastructure.\n\n## Acceptance\n\n- `/reload-plugins` after the fix shows 4 hooks again (or whatever the pre-plugin baseline was)\n- Only user-level state-tracker.sh fires on events\n- Plugin MCP server, skills, and slash commands still work","notes":"## prepared.prompt\n\n### Context\nAfter installing the conductor-mcp plugin, every Claude Code event fires state-tracker.sh twice — once from `~/.claude/settings.json` and once from the plugin's bundled `hooks.json`. They write the same `/tmp/claude-code-state/*.json` file (racy). The plugin's bundled state-tracker.sh is also 286 lines vs the user-level 509-line authoritative copy (per cm-7jr findings). The plugin should not own this cross-cutting script.\n\n### Task\nRemove the plugin's hook registration so only the user-level state-tracker fires.\n\n### Files\n- `plugins/conductor/hooks/hooks.json` — delete (or empty the hooks object)\n- `plugins/conductor/hooks/scripts/state-tracker.sh` — delete (users provide their own at ~/.claude/)\n- `plugins/conductor/hooks/statusline-script.sh` — delete if present\n- `plugins/conductor/hooks/` — remove the directory entirely if nothing remains\n- `plugins/conductor/.claude-plugin/plugin.json` — verify no hooks refs to remove\n\n### Verify\n- After change, `/reload-plugins` drops hook count from 12 back to pre-plugin baseline (expect ~4)\n- Conductor MCP server, skills, and slash commands still work\n- User-level hooks from ~/.claude/settings.json still fire\n\n### When Done\n\\`bd close cm-9vs --reason 'plugin hooks unhooked; user-level state-tracker is sole source'\\`\n## Retro\n- What worked: plugin.json had no hooks refs so a clean `rm -rf plugins/conductor/hooks/` was sufficient; reference/ already holds the authoritative copies of state-tracker.sh and audio-announcer.sh for users to install at ~/.claude/.\n- What was unclear: whether statusline-script.sh or audio-announcer.sh were wired anywhere else — grep confirmed they were only referenced by the deleted hooks.json and state-tracker.sh, so removing the whole hooks/ dir was safe.","status":"closed","priority":1,"issue_type":"bug","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-23T15:18:38Z","created_by":"GGPrompts","updated_at":"2026-04-23T15:45:58Z","closed_at":"2026-04-23T15:45:58Z","close_reason":"plugin hooks unhooked; user-level state-tracker is sole source","labels":["hooks","plugin","ready"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"cm-d64","title":"Decide: unified config format + audio-shim strategy","description":"## Why\n\nThree separate config stores today:\n- `~/.config/conductor-mcp/config.json` — MCP server (JSON)\n- `~/.config/conductor-tui/config.yaml` — TUI (YAML)\n- `~/.claude/audio-config.sh` — hook scripts (bash-sourced)\n\ncm-3gw assumes config.json is the single source. But audio settings must remain readable by shell hooks when the MCP server is not attached. Need a decision before implementing the TUI settings tab or audio bridge.\n\n## Decisions needed\n\n1. **Canonical file path** — e.g. `~/.config/conductor/config.json` with sections `audio`, `tui`, `mcp`, `profiles`. Or keep existing `~/.config/conductor-mcp/config.json` and add audio section.\n2. **Format** — JSON (MCP already uses; `jq` reads cleanly from bash) vs YAML (TUI uses; harder from bash).\n3. **Hook consumption strategy**:\n   - (a) `jq`-direct: hook scripts call `jq` on the canonical file. Clean, but hard dep on `jq`.\n   - (b) Regenerated `.sh` shim: TUI/MCP regenerate `audio-config.sh` whenever audio settings change. Hooks keep shell-sourcing it. No jq dep, but adds \"who-writes-last\" invariant.\n4. **Migration** — how `~/.config/conductor-mcp/config.json`, `conductor-tui/config.yaml`, and `audio-config.sh` converge. One-shot migrator on first run?\n\n## Output\n\nShort decision doc in this issue's notes. Unblocks cm-3gw final design and the audio bridge task.","design":"# Decision: Unified Config Format and Audio-Shim Strategy\n\n## Canonical path\n\n**`~/.config/conductor/config.json`**\n\nRationale: the namespace `conductor-mcp` is an implementation detail; the user-facing product is \"conductor\". Both the MCP server and TUI are components of the same tool, and their configs should live together. A shared dir also makes migration and future additions (e.g. a daemon) straightforward without new directories.\n\nLegacy paths kept as **read-once fallback** on first boot:\n- `~/.config/conductor-mcp/config.json` → merge into `mcp` + `audio` + `profiles` sections\n- `~/.config/conductor-tui/config.yaml` → merge into `tui` section\n- `~/.claude/audio-config.sh` → merge into `audio` section\n\nNeither MCP server nor TUI writes to the legacy paths after migration.\n\n## Format\n\n**JSON** throughout.\n\n- MCP server already reads/writes JSON (`server.py` line 26: `CONFIG_FILE = CONFIG_DIR / \"config.json\"`)\n- `jq` 1.7 is present on this system (`/usr/bin/jq`) — confirmed via `command -v jq`\n- YAML (current TUI format) requires a Go YAML library; JSON can be parsed by `encoding/json` with zero extra deps and by `jq` from bash with zero extra deps\n- The YAML config currently carries only display/layout preferences that are easily expressed in JSON; no multi-line strings or anchors are in use\n- Human-editability: JSON with 2-space indent is acceptable for this audience; both formats are roughly equivalent for the key count here\n\n## Hook consumption\n\n**Option (a): `jq`-direct.** Hook scripts (`audio-announcer.sh`) read the canonical JSON file directly via `jq`.\n\nReasons to prefer (a) over (b):\n- `jq` 1.7 is already installed — no new dependency to document for this machine\n- Option (b) requires two independent writers (MCP Python + TUI Go) to each implement `write_audio_shim()` and call it on every audio-setting mutation. Any missed call site silently desynchronises the shim. That is a latent bug surface with no upside once `jq` is confirmed present.\n- The shim approach requires `audio-config.sh` to remain a persistent artefact that can be edited by users and then silently overwritten — a UX footgun.\n- `jq`-direct reads are atomic (single `jq` call per hook invocation) and always reflect the current state of the canonical file without a two-step write-then-source cycle.\n\nFallback for environments without `jq`: keep `~/.claude/audio-config.sh` as a static fallback, sourced when the canonical JSON is absent or `jq` is unavailable. Add a header comment: `# Fallback — edit ~/.config/conductor/config.json audio section instead`. This preserves backward compatibility without mandating the shim write path.\n\n`jq` listed as a required dependency in `CLAUDE.md` (alongside `tmux`, `edge-tts`, `git`).\n\n## Migration\n\n**One-shot migrator on first run** inside `server.py` `load_config()`:\n\n1. If `~/.config/conductor/config.json` exists → load it, done.\n2. Else: collect legacy fragments:\n   a. Read `~/.config/conductor-mcp/config.json` if present.\n   b. Parse `~/.config/conductor-tui/config.yaml` if present (Python `yaml` or line-by-line for the simple fields).\n   c. Source-parse `~/.claude/audio-config.sh` (regex or `bash -c 'source file; env'` subprocess) if present.\n3. Merge into canonical schema, write `~/.config/conductor/config.json`.\n4. Log \"Migrated config to ~/.config/conductor/config.json\" once.\n5. Do NOT delete legacy files — leave them in place so other tooling that hasn't been updated yet doesn't break. Add a `# Migrated — see ~/.config/conductor/config.json` comment to `audio-config.sh` if writable.\n\nTUI performs the same read-canonical-or-migrate on startup. Both components share migration logic where practical (or each migrates independently — the merge is idempotent because the canonical file is the gate).\n\n## Schema sketch\n\n```json\n{\n  \"mcp\": {\n    \"max_concurrent_workers\": 4,\n    \"default_layout\": \"2x2\",\n    \"min_pane_width\": 80,\n    \"min_pane_height\": 24,\n    \"conductor_mode\": \"session\",\n    \"send_keys_delay_ms\": 800,\n    \"claude_boot_delay_s\": 3.0,\n    \"default_dir\": \"~\"\n  },\n  \"audio\": {\n    \"default_voice\": null,\n    \"voice_rate\": \"+20%\",\n    \"voice_pitch\": \"+0Hz\",\n    \"volume\": \"+0%\",\n    \"playback_speed\": 1.0,\n    \"random_voices\": true,\n    \"announce_tools\": true,\n    \"announce_session_start\": true,\n    \"announce_ready\": true,\n    \"tool_debounce_ms\": 1000,\n    \"custom_clips_dir\": null\n  },\n  \"tui\": {\n    \"theme\": \"dark\",\n    \"custom_theme\": {},\n    \"keybindings\": \"default\",\n    \"custom_keybindings\": {},\n    \"layout\": { \"type\": \"single\", \"split_ratio\": 0.5, \"show_divider\": true },\n    \"ui\": { \"show_title\": true, \"show_status\": true, \"mouse_enabled\": true, \"show_icons\": true, \"icon_set\": \"nerd_font\" },\n    \"performance\": { \"lazy_loading\": true, \"cache_size\": 100, \"async_operations\": true },\n    \"logging\": { \"enabled\": false, \"level\": \"info\", \"file\": \"~/.local/share/conductor-tui/debug.log\" }\n  },\n  \"profiles\": {\n    \"claude\":   { \"command\": \"claude\",    \"dir\": null },\n    \"codex\":    { \"command\": \"codex\",     \"dir\": null },\n    \"gemini\":   { \"command\": \"gemini -i\", \"dir\": null },\n    \"copilot\":  { \"command\": \"copilot\",   \"dir\": null },\n    \"lazygit\":  { \"command\": \"lazygit\",   \"dir\": null },\n    \"tfe\":      { \"command\": \"tfe\",       \"dir\": null }\n  },\n  \"voices\": {}\n}\n```\n\nType notes:\n- All string fields: `string | null` (null = use default/random)\n- `mcp.send_keys_delay_ms`: integer ms\n- `audio.playback_speed`: float (mpv multiplier)\n- `profiles`: map of `{ command: string, dir: string | null }`\n- `voices`: map of worker-name → assigned voice string (runtime, managed by MCP server)\n- `tui.custom_theme` and `tui.custom_keybindings`: objects, only present when mode is \"custom\"","notes":"## prepared.prompt\n\n### Context\nThree config stores exist: `~/.config/conductor-mcp/config.json` (MCP, JSON), `~/.config/conductor-tui/config.yaml` (TUI, YAML), `~/.claude/audio-config.sh` (hook-sourced bash). Hooks must read audio settings when the MCP server is NOT attached. This is a DECISION issue — output a short recommendation doc, not code.\n\n### Task\nWrite a concrete decision in this issue's design field covering:\n\n1. **Canonical file path** — pick one (suggest `~/.config/conductor/config.json`)\n2. **Format** — JSON vs YAML. Recommendation: JSON (MCP already uses it, jq reads cleanly)\n3. **Hook consumption** — (a) jq-direct vs (b) regenerated .sh shim. Check whether jq is already installed.\n4. **Migration** — how the three existing files converge. One-shot migrator on first run? Leave legacy paths as fallback?\n5. **Schema sketch** — top-level sections (`audio`, `tui`, `mcp`, `profiles`), roughly typed.\n\n### Reference\n- cm-7jr notes — full consumer inventory\n- Current files: `~/.config/conductor-mcp/config.json`, `~/.config/conductor-tui/config.yaml`, `~/.claude/audio-config.sh`\n\n### When Done\nWrite decision into `design` field via `bd update cm-d64 --design ...`. Then `bd close cm-d64 --reason '\u003cone-line summary of decision\u003e'`. Unblocks cm-2k1 and cm-3gw.\n\n## Retro\n- What worked: all three legacy config files were readable (audio-config.sh was the only one present on this device), `jq` version confirmed in one command, server.py config path confirmed at line 26. Consumer inventory from cm-7jr was complete and directly applicable.\n- What was unclear: whether the TUI config.yaml had been written to disk yet (it hadn't — only the example exists in code). This simplified the migration story since there are effectively zero live TUI config files to migrate today.","status":"closed","priority":1,"issue_type":"task","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-23T15:18:23Z","created_by":"GGPrompts","updated_at":"2026-04-23T15:46:34Z","closed_at":"2026-04-23T15:46:34Z","close_reason":"canonical ~/.config/conductor/config.json JSON; hooks use jq-direct with static sh fallback; one-shot migrator on first run","labels":["audio","config","decision","ready"],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"id":"cm-7jr","title":"Map state-tracker.sh consumers across projects","description":"## Why\n\n`~/.claude/hooks/scripts/state-tracker.sh` is 509 lines, customized, and used by apps beyond conductor-mcp. Before unifying config (see cm-3gw) we need a clear picture of who reads what, so we don't break unrelated consumers when refactoring audio/config paths.\n\n## Scope\n\nAudit across `~/projects` and `~/.claude` for:\n\n1. **Readers of `/tmp/claude-code-state/*.json`** — which tools/TUIs/scripts parse this?\n2. **Sources of `~/.claude/audio-config.sh`** — which hook/shell scripts depend on the env-var format?\n3. **Callers of `state-tracker.sh`** — `~/.claude/settings.json` hooks, plugin `hooks.json` files, standalone scripts\n4. **State-file writers other than state-tracker.sh** — anyone else writing `/tmp/claude-code-state`?\n\n## Output\n\nA table in the issue notes with columns: consumer, file, what it needs, format assumption. Drives the audio-bridge design in the format/shim decision (blocks #3 audio bridge).","notes":"## Investigation output (captured 2026-04-23)\n\n### Copies of state-tracker.sh on this device\n\n| Path | Lines | Role |\n|---|---|---|\n| `~/.claude/hooks/scripts/state-tracker.sh` | 509 | **Authoritative / authoring copy** — user edits here directly. Includes branches for tmux AND for `~/projects/therminal` (a GUI terminal without tmux). |\n| `~/plugins/my-plugins/plugins/audio-alerts/hooks/scripts/state-tracker.sh` | 300 | Personal `audio-alerts` plugin source — stale by 209 lines |\n| `~/projects/conductor-mcp/plugins/conductor/hooks/scripts/state-tracker.sh` | 286 | Open-source `conductor-mcp` plugin source — stale |\n| `~/projects/pocketforge/forge/scripts/state-tracker.sh` + APK asset copy | 258 each | Self-contained for phone (Termux fork). Uses `termux-tts-speak` (Android native TTS) instead of edge-tts — fundamentally different audio stack |\n| `~/.claude/plugins/cache/...` | n/a | Auto-regenerated on plugin install |\n\n### Hook registration sources\n\n- `~/.claude/settings.json` — user-level, registers 8 hooks → `~/.claude/hooks/scripts/state-tracker.sh`. **This is the current source of truth on desktop.**\n- `~/plugins/my-plugins/plugins/audio-alerts/hooks/hooks.json` — byte-identical 8-hook registration; would duplicate if installed\n- `~/projects/conductor-mcp/plugins/conductor/hooks/hooks.json` — same byte-identical 8-hook registration; currently IS installed and causing duplication (separate issue cm-9vs)\n- `~/projects/pocketforge/.../install-hooks.sh` — installs its own bundled copies on the phone; not coupled to desktop\n\n### Ownership decision\n\n1. **Desktop (this device):** `~/.claude/hooks/scripts/state-tracker.sh` is the single source of truth. Edit in place.\n2. **Personal plugins (`audio-alerts`):** Mirror the master via `/sync` propagation (see new sync-propagation issue). Plugin's `hooks.json` stays as-is but will conflict if installed — user's `settings.json` is the true hook registrar.\n3. **Open-source plugins (`conductor-mcp`):** Do NOT mirror. Plugin should not ship or register state-tracker.sh at all (see cm-9vs). Users provide their own.\n4. **Mobile (`pocketforge`):** Self-contained by design. Different TTS backend (`termux-tts-speak`), different feature set. OUT OF SCOPE for unification.\n5. **Other devices (laptop/phone non-authoring):** Receive updates via plugin repo pull only. No reverse-propagation — confirmed with user.\n\n### Why the drift\n\nThe 509-line version's extra lines are primarily therminal support — a GUI terminal project under development at `~/projects/therminal` that doesn't use tmux. Any cleanup must preserve those branches.\n\n### Consumers of `/tmp/claude-code-state/*.json`\n\n- `conductor-tui` (reads state files for session status display; see `tui/claude_state.go`)\n- `state-tracker.sh` itself (writes, and re-reads for debounce/context linking)\n- Any additional consumers to audit if this becomes important\n\n### Consumers of `~/.claude/audio-config.sh`\n\n- `~/.claude/hooks/scripts/audio-announcer.sh` (sources it for voice/rate/toggles)\n- `/audio` slash command (`~/.claude/commands/audio.md`) — writes it\n- **Not** used by pocketforge (different audio stack)","status":"closed","priority":1,"issue_type":"task","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-23T15:18:13Z","created_by":"GGPrompts","updated_at":"2026-04-23T15:35:53Z","closed_at":"2026-04-23T15:35:53Z","close_reason":"Investigation complete. Full findings captured in issue notes: copy inventory, ownership decisions (desktop=~/.claude authoritative, personal plugins mirror via /sync, conductor-mcp drops bundled copy, pocketforge self-contained), consumers of state file and audio-config.sh, reason for 509-line drift (therminal GUI terminal support).","labels":["audio","config","hooks","investigation"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"cm-8k0s","title":"Cache health monitor — warn when session cache is broken via status line + audio","description":"Confirmed cache bug in CC: attestation data changes per request, invalidating the cache hash and causing 10-20x token burn. Users have no visibility into whether their session's cache is working. \n\nAdd a post-tool hook that monitors cache metrics from JSONL session logs. When cache_read is stuck at zero while cache_creation keeps growing, surface a warning in the status line and announce via audio that the session cache is broken and the user should start a new session.\n\nThis is safe — no proxying, no intercepting API calls. Just reading the usage field from JSONL entries that CC already writes locally.\n\nReferences:\n- Cache bug: attestation data invalidates cache hash every request\n- JSONL entries contain usage.cache_read_input_tokens and usage.cache_creation_input_tokens\n- Anthropic confirmed users hitting limits \"way faster than expected\"\n- https://piunikaweb.com/2026/03/31/claude-cache-bugs-tokens-20x-more-anthropic-investigating/","design":"1. Post-tool hook reads the latest JSONL entry from the active session log\n2. Parse usage field for cache_read_input_tokens and cache_creation_input_tokens\n3. Track rolling window (last N entries) — if cache_read is consistently 0 while cache_creation is high, flag it\n4. Write warning state to /tmp/claude-code-state/cache-warning.json for status line pickup\n5. Trigger audio alert via thermal-audio socket: \"Cache is broken, consider starting a new session\"\n6. Optional: track cache hit ratio over session lifetime and surface in status line (e.g. \"Cache: 92%\" or \"Cache: 0% ⚠\")","acceptance_criteria":"- Hook detects broken cache pattern (zero reads, growing creation) within 3-5 tool calls\n- Status line shows cache health indicator\n- Audio announces warning when cache breaks\n- No false positives on session start (first few calls always have cache_creation)\n- Works with both CC and Codex sessions","notes":"## prepared.prompt\n\n## Context\nConfirmed CC cache bug: attestation data changes per request, invalidating the cache hash and causing 10-20x token burn. Users have no visibility. Goal: passive observer — read CC's local JSONL session logs, surface a warning via status line + audio. No proxying, no intercepting API calls.\n\nJSONL sessions live at `~/.claude/projects/\u003cproject-hash\u003e/\u003csession-id\u003e.jsonl` with per-entry `usage.cache_read_input_tokens` + `usage.cache_creation_input_tokens` fields.\n\nHook infra to extend:\n- `~/.claude/hooks/scripts/state-tracker.sh` already runs on PostToolUse (1s timeout, see ~/.claude/settings.json).\n- `~/.claude/hooks/statusline-script.sh` renders the status line.\n- `~/.claude/hooks/scripts/audio-announcer.sh` is the audio alert path.\n- Canonical sources are in `reference/` in this repo; `sync-hook-scripts.sh` mirrors them to `~/.claude/hooks/scripts/`.\n\nRequires cm-y7t first so `voice.enabled` gates the audio alert correctly.\n\n## Task\n\n### 1. Cache-health check in state-tracker.sh post-tool\nExtend `reference/state-tracker.sh` post-tool branch:\n- Find the active JSONL: current project hash dir + most recently mtime'd `*.jsonl` file.\n- Read last 5 entries. Extract `usage.cache_read_input_tokens` / `usage.cache_creation_input_tokens`.\n- Detection: cache_read == 0 across all 5 entries AND cache_creation grew on at least 3 of them.\n- False-positive guard: skip check if session has fewer than 8 total entries (cache_creation is always nonzero at session start).\n\n### 2. Warning state file\n- On detection: write `/tmp/claude-code-state/cache-warning.json` with `{\"session_id\": \"...\", \"detected_at\": \"...\", \"recent_cache_read\": 0, \"recent_cache_creation\": N}`.\n- On recovery (last entry has cache_read \u003e 0): delete the file.\n\n### 3. Status-line integration\nExtend `reference/tmux-status-claude.sh` (and `~/.claude/hooks/statusline-script.sh`):\n- If cache-warning.json exists → append `Cache: 0% ⚠`.\n- Else → compute cache hit ratio from current session totals, show `Cache: NN%`.\n\n### 4. Audio alert (gated)\nOn detection, if `voice.enabled` is true (use the cm-y7t jq bridge), fire `audio-announcer.sh` once with \"Cache is broken, start a new session\". Rate-limit to once per 10 minutes per session via a marker file mtime (`/tmp/claude-code-state/cache-alert-\u003csession-id\u003e.marker`).\n\n### 5. Mirror to reference/\nUpdate `reference/state-tracker.sh` and `reference/tmux-status-claude.sh` canonically in this repo — they are the sources of truth that `sync-hook-scripts.sh` copies to `~/.claude/hooks/scripts/`.\n\n## Key Files\n- reference/state-tracker.sh (post-tool branch)\n- reference/tmux-status-claude.sh (status line rendering)\n- reference/audio-announcer.sh (existing audio alert caller — invoke from state-tracker)\n- `~/.claude/hooks/scripts/state-tracker.sh` (live copy — either edit both or run sync after editing reference/)\n- Test data: `~/.claude/projects/*/session.jsonl` to see real schema\n\n## docs:update\nUpdate root CLAUDE.md with ONLY:\n- Cache-health monitor runs from the PostToolUse hook, surfaces breakage via status line + rate-limited audio.\n- Warning state file path: `/tmp/claude-code-state/cache-warning.json`.\nNo implementation detail, no script paths.\n\n## Verify\n1. Normal session → status line shows `Cache: NN%`.\n2. Simulate broken cache: inject 5 synthetic JSONL entries with cache_read=0, cache_creation\u003e0 → cache-warning.json appears → status line flips → audio fires once (when voice.enabled=true).\n3. Set voice.enabled=false (cm-y7t) → status line still flips, audio stays silent.\n4. Recover: append entry with cache_read\u003e0 → warning file removed → status line returns to percent.\n5. Rate-limit: simulate detection twice within 60s → audio fires only once.\n\n## When Done\nbd close cm-8k0s --reason \"cache-health monitor active via PostToolUse hook; status line + audio alert rate-limited\"\n## Retro\n- What worked: Synthesized JSONL fixture (10 entries, 5 broken + 1 healthy recovery) verified detection + recovery + voice-gate paths cleanly against reference/state-tracker.sh directly. JSONL schema matched exactly (assistant entries with message.usage.cache_read_input_tokens / cache_creation_input_tokens). Dashified cwd naming (/ -\u003e -) for the ~/.claude/projects dirname worked first try.\n- What was unclear: Project-relative 'mirror to ~/.claude/hooks/scripts/' instruction collides with this device's layout: the live state-tracker.sh is a symlink to therminal/ and the live audio-announcer.sh is the audio-alerts plugin's variant — both diverged from conductor-mcp/reference/. sync-manifest.json explicitly excludes conductor-mcp (cm-9vs). Kept edits to reference/*.sh only and verified via direct invocation; did not clobber the therminal/audio-alerts variants. Users who install per README (cp reference/state-tracker.sh ~/.claude/hooks/scripts/) get the full feature.\n- Followups: on a device where conductor's state-tracker IS the live hook, the added block fires on every post-tool (reads whole JSONL via jq — acceptable at 1s hook budget for sessions \u003cfew-MB, may want a tail-based fast path for huge logs).","status":"open","priority":1,"issue_type":"feature","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-02T17:51:10Z","created_by":"GGPrompts","updated_at":"2026-04-23T18:10:10Z","labels":["cache","docs:update","hooks","observability","ready"],"dependencies":[{"issue_id":"cm-8k0s","depends_on_id":"cm-5cl","type":"blocks","created_at":"2026-04-23T13:53:01Z","created_by":"GGPrompts","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"cm-06j","title":"TUI Settings Voice section: rate/pitch editing, random toggle, reset assignments","description":"cm-3gw shipped the Voice picker but left four user-facing capabilities unreachable after the MCP tools were removed. The canonical config at `~/.config/conductor/config.json` already has fields for all of them — the TUI just lacks the editing UI.\n\n## Gaps (confirmed against current canonical config)\n\nCurrent canonical config shape:\n```json\n{\n  \"voice\": {\n    \"default\": \"en-GB-SoniaNeural\",\n    \"rate\": \"+15%\",\n    \"pitch\": \"+0Hz\",\n    \"random_per_worker\": true\n  }\n}\n```\n\n| Capability | Removed MCP tool | TUI state today |\n|---|---|---|\n| Edit voice rate (e.g. `+15%` → `+25%`) | `set_config(voice_rate=...)` | Displayed read-only in Voice section header |\n| Edit voice pitch (e.g. `+0Hz` → `-5Hz`) | `set_config(voice_pitch=...)` | Displayed read-only in Voice section header |\n| Toggle random voice per worker (`voice.random_per_worker`) | `set_config(random_voices=...)` | Not shown at all |\n| Reset per-worker voice assignments | `reset_voice_assignments()` | Not reachable |\n\n## Schema note\n\nConfig key is `voice.random_per_worker` (current canonical). The removed MCP tool called it `random_voices`. Keep the new name — don't reintroduce the old identifier in the TUI.\n\n## Fix sketch\n\nIn `tui/settings.go`/`tui/model.go` `updateSettingsContent`, under `settingsSectionVoice`:\n- Add editable rows above the voice pool for rate (`+XX%` stepper) and pitch (`+YYHz` stepper). Re-use cursor semantics — selected row accepts left/right to adjust, enter to save.\n- Add a toggle row for `random_per_worker` (on/off with spacebar).\n- Add a \"Reset voice assignments\" action row that clears the per-worker mapping stored in `~/.config/conductor/state.json` (or wherever server.py persists it — verify location in server.py's assignment code).\n\nIn `tui/update_keyboard.go` `handleSettingsKeys`:\n- Extend voice section handling so up/down moves between editable rows + voice pool; left/right adjusts the focused scalar field; space toggles the boolean.\n- Coordinate with cm-cjk (scroll fix) since voice section will grow.\n\n## Verification\n\n- Edit rate in TUI → `jq '.voice.rate' ~/.config/conductor/config.json` reflects → next `speak(...)` call uses new rate.\n- Toggle random_per_worker off → two consecutive spawns get the same voice.\n- Reset assignments → next spawn assigns fresh voice.\n\n## Dependencies\n\n- cm-cjk (scroll) — without the scroll fix, new editable rows push voice pool further off-screen.\n- cm-3gw (complete) — this issue exists because cm-3gw removed the tools.","notes":"## prepared.prompt\n\n## Context\ncm-3gw removed set_config and reset_voice_assignments MCP tools, but the TUI Voice section only lets users pick a voice — editing rate/pitch, toggling random_per_worker, and clearing per-worker voice assignments are unreachable.\n\nVoice assignments are stored **in the canonical config itself** under `worker_voice_assignments` (worker_id → voice), not a separate state file. See server.py:78, 225-267.\n\ncm-cjk fixes scroll tracking; new editable rows added here rely on that working.\n\n## Task\n\n### Add editable rows above the voice pool\n- Rate: `+XX%` stepper (left/right adjusts ±5%, enter saves). Read/write voice.rate.\n- Pitch: `+YYHz` stepper (left/right adjusts ±5Hz, enter saves). Read/write voice.pitch.\n- Random per worker: toggle (space flips). Read/write voice.random_per_worker. **Keep the new name — do not reintroduce the old random_voices key.**\n\n### Add action row below the voice pool\n- Reset voice assignments: enter clears worker_voice_assignments map in canonical config; status bar confirms.\n\n### Keyboard handling (tui/update_keyboard.go handleSettingsKeys)\n- Extend cursor range: [rate row, pitch row, random-toggle row, voice pool entries..., reset-action row].\n- Left/right arrows adjust scalar fields only when cursor is on rate or pitch.\n- Space toggles boolean when cursor is on random-toggle row.\n- Enter: on voice pool entry → existing save-and-preview; on reset-action row → clear assignments.\n\n## Key Files\n- tui/settings.go (add saveVoiceRate, saveVoicePitch, saveRandomPerWorker, resetVoiceAssignments helpers)\n- tui/model.go (updateSettingsContent voice section, 776-802)\n- tui/update_keyboard.go (handleSettingsKeys, 353-427)\n- server.py (reference: 225-267 for how worker_voice_assignments is stored)\n\n## Verify\n1. cd tui \u0026\u0026 go build -o conductor-tui .\n2. Edit rate from +15% to +25%, save. `jq .voice.rate ~/.config/conductor/config.json` → +25%.\n3. Toggle random_per_worker off. smart_spawn twice → both workers get the default voice.\n4. Reset. `jq .worker_voice_assignments ~/.config/conductor/config.json` → {}.\n5. Scroll still works after adding rows (regression check for cm-cjk).\n\n## When Done\nbd close cm-06j --reason \"voice rate/pitch/random/reset exposed in TUI Settings\"\n## Retro\n- What worked: Preserving voicePreambleLines=7 (the first cursor row still lands on line 7) and adding a voiceCursorLine() helper to absorb the blank+header gaps before the voice pool and before the reset row kept the scroll math local. Added a go test (settings_voice_test.go) that walks every cursor position and asserts the rendered line has a SELECTED: prefix — this is a durable regression check for cm-cjk's invariant.\n- What was unclear: The issue said 'left/right adjusts, enter saves' but writing to config immediately on left/right feels more natural for a stepper, so I did immediate-save and re-purposed Enter on rate/pitch rows to preview the current voice with the new values. Flag if this differs from intent.\n- Could not interactively verify the UX (per rules). Verified via go build, go vet, go test ./..., and careful cursor-to-line math review.","status":"closed","priority":2,"issue_type":"feature","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-23T16:53:42Z","created_by":"GGPrompts","updated_at":"2026-04-23T17:13:34Z","closed_at":"2026-04-23T17:13:34Z","close_reason":"voice rate/pitch/random/reset exposed in TUI Settings","labels":["follow-up","ready","settings","tui"],"dependencies":[{"issue_id":"cm-06j","depends_on_id":"cm-3gw","type":"blocks","created_at":"2026-04-23T12:53:42Z","created_by":"GGPrompts","metadata":"{}"},{"issue_id":"cm-06j","depends_on_id":"cm-cjk","type":"blocks","created_at":"2026-04-23T12:53:42Z","created_by":"GGPrompts","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"id":"cm-b6r","title":"conductor-tui Settings: Profiles CRUD + Timing editor","description":"Follow-up from cm-3gw. Settings tab MVP shipped with Voice section (picker + preview + save). Profiles and Layout/Timing sub-sections show current values but have no editing UI.\n\nScope:\n1. Profiles CRUD: add/edit/delete with modal input flow (reuse rename modal pattern in tui/update_keyboard.go). Writes to ~/.config/conductor/config.json 'profiles' section.\n2. Timing editor: default_layout, default_dir, send_keys_delay_ms, claude_boot_delay_s.\n3. Audio schema migration: update tui/settings.go once cm-2k1 finalizes the audio section name (currently dual-reads voice + audio).\n\nFiles:\n- tui/settings.go, tui/update_keyboard.go, tui/model.go updateSettingsContent\n\nVerify:\n- Edit profile in TUI -\u003e canonical config reflects -\u003e smart_spawn(profile=...) picks it up\n- Edit send_keys_delay_ms -\u003e server.py reads on next call","notes":"## prepared.prompt\n\n## Context\ncm-3gw removed add_profile, remove_profile, list_profiles, and the set_config timing/layout params. The Profiles section in TUI Settings shows names only; Timing displays values read-only. This issue completes the editing flow that was the reason for the MCP tool removal.\n\nExisting rename modal pattern in tui/update_keyboard.go:44-74 (`inputMode == \"rename\"`, renameTarget, esc cancels, enter confirms) — adapt this pattern for profile add/edit flows.\n\n## Task\n\n### Profiles section\n- Add: cursor on \"+ Add profile\" row → enter starts modal → type name → enter creates stub entry (empty cmd/desc) in config profiles map.\n- Edit: cursor on existing profile → enter cycles into field-edit mode (name, command, description).\n- Delete: cursor on existing profile → `d` prompts \"Delete X? (y/n)\" in status bar for confirmation.\n\n### Timing section\n- Cursor traverses: [default_layout, default_dir, send_keys_ms, claude_boot_s].\n- Enter on any field → modal text input. Save writes to canonical config.\n- Validate: default_layout matches `^[1-9]x[1-9]$`, default_dir non-empty, ms/s are integers.\n\n### Schema\nWrites to ~/.config/conductor/config.json: top-level `profiles`, `default_layout`, `default_dir`, `delays.send_keys_ms`, `delays.claude_boot_s`.\n\n## docs:update\nAfter implementation, update the root CLAUDE.md with ONLY:\n- Note that Profile CRUD and Timing editing are available in TUI Settings (not just Voice picker).\nNo file paths, no build commands — discoverable from code.\n\n## Key Files\n- tui/settings.go (add saveProfile, deleteProfile, saveTimingField)\n- tui/model.go (updateSettingsContent profiles+timing sections, 804-845)\n- tui/update_keyboard.go (extend handleSettingsKeys + inputMode handling at 44-74)\n- tui/types.go (add profile/timing input state fields as needed)\n\n## Verify\n1. cd tui \u0026\u0026 go build -o conductor-tui .\n2. Add profile \"test-claude\" with cmd `claude --model sonnet`. `jq .profiles ~/.config/conductor/config.json`.\n3. Call MCP smart_spawn(profile=\"test-claude\") → worker launches with that command.\n4. Edit send_keys_ms from 800 → 1000. Next send_keys call respects the new value.\n5. Delete \"test-claude\" → gone from config.\n\n## When Done\nbd close cm-b6r --reason \"Profiles CRUD + Timing editor completed in TUI\"\n## Retro\n- What worked: The cm-cjk/cm-06j pattern (cursorPreambleLines + cursorLine helpers + SELECTED: row tagging) cleanly extended to Profiles and Timing. Table-driven cursor tests per-section caught the preamble offsets on the first run.\n- What worked: Reusing inputMode for the edit wizard (name→command→description) let us keep all text-capture modes in one shared handler block — added 5 new modes with minimal branching.\n- What worked: timingFieldOrder slice cleanly maps cursor index → field key, avoiding a switch in 4 places.\n- What was unclear: Whether 'edit' should expand inline or use the status-bar modal. Went with modal (status bar) to match existing rename/delete flows; inline editing would have required deeper layout surgery.\n- What was unclear: Whether to cycle all 3 fields on one Enter or require a second Enter between fields. Went with the former since the task said 'cycles into field-edit mode'.\n- Could not interactively verify: Per task rules. Covered via cursor-line tests + CRUD roundtrip test + timing validation table-driven test. All 8 TUI tests pass under go test ./....","status":"closed","priority":2,"issue_type":"feature","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-23T16:03:14Z","created_by":"GGPrompts","updated_at":"2026-04-23T17:20:44Z","closed_at":"2026-04-23T17:20:44Z","close_reason":"Profiles CRUD + Timing editor completed in TUI","labels":["docs:update","follow-up","ready","settings","tui"],"dependencies":[{"issue_id":"cm-b6r","depends_on_id":"cm-06j","type":"blocks","created_at":"2026-04-23T12:58:33Z","created_by":"GGPrompts","metadata":"{}"},{"issue_id":"cm-b6r","depends_on_id":"cm-cjk","type":"blocks","created_at":"2026-04-23T12:53:52Z","created_by":"GGPrompts","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"id":"cm-dl4","title":"Add state-tracker.sh propagation step to /sync command","description":"## Why\n\n`~/.claude/hooks/scripts/state-tracker.sh` is the authoring copy on this device. Personal plugins (`audio-alerts`) have their own source copies that drift. Symlinks would break plugin portability and cross-device pulls. A propagation step in `/sync` (device-specific, this device only) keeps plugin source files in sync before push — plugins stay portable, other devices receive updates through normal plugin repo pulls.\n\nScoped out: conductor-mcp (per cm-9vs, no longer bundles this script) and pocketforge (self-contained, different audio stack).\n\n## Scope\n\nAdd a new step to `~/.claude/commands/sync.md` (between the current Step 3 and Step 4, i.e., after pull, before push) that:\n\n1. Reads a manifest (e.g., `~/.claude/hooks/sync-manifest.json`) listing `master → mirrors[]`\n2. For each mirror: diffs master vs mirror; if different, copies master to mirror\n3. Skips mirrors whose containing repo has uncommitted changes the user hasn't addressed yet (matches sync's \"don't silently discard work\" rule)\n4. Reports what was propagated so the user sees it in the summary\n\nNo reverse propagation — user confirmed this device is the only authoring device. Other devices pull the plugin repo and pick up updates the normal way.\n\n## Manifest shape (starting point)\n\n```json\n{\n  \"scripts\": [\n    {\n      \"master\": \"~/.claude/hooks/scripts/state-tracker.sh\",\n      \"mirrors\": [\n        \"~/plugins/my-plugins/plugins/audio-alerts/hooks/scripts/state-tracker.sh\"\n      ]\n    }\n  ]\n}\n```\n\nExtensible: can add audio-announcer.sh or other shared scripts later by appending entries.\n\n## Acceptance\n\n- Edit master; run `/sync`; mirror in `audio-alerts` plugin shows the same bytes\n- Commit + push of `audio-alerts` picks up the propagated file naturally\n- Running `/sync` when mirror's containing repo has uncommitted work skips with a clear report (no silent clobber)\n- Pocketforge and conductor-mcp are NOT in the manifest and are NOT touched\n\n## Notes\n\n- Device-specific command — user will not add this step to `/sync` on other devices\n- Consider a small Python or bash helper script `sync-hook-scripts.sh` invoked from sync.md, rather than embedding all logic inline in the markdown command","notes":"## prepared.prompt\n\n### Context\n\\`~/.claude/hooks/scripts/state-tracker.sh\\` is the authoring copy on this device (509 lines, includes therminal GUI terminal support). Personal plugins like \\`audio-alerts\\` have their own source copies that need to stay in sync. Symlinks would break plugin portability when repos are cloned on other devices. User confirmed this device is the sole authoring device — no reverse propagation.\n\n### Task\nAdd a propagation step to \\`~/.claude/commands/sync.md\\` between current Step 3 (pull) and Step 4 (push):\n\n1. **Create manifest** \\`~/.claude/hooks/sync-manifest.json\\`:\n   \\`\\`\\`json\n   {\n     \"scripts\": [\n       {\n         \"master\": \"~/.claude/hooks/scripts/state-tracker.sh\",\n         \"mirrors\": [\"~/plugins/my-plugins/plugins/audio-alerts/hooks/scripts/state-tracker.sh\"]\n       }\n     ]\n   }\n   \\`\\`\\`\n\n2. **Helper script** (recommended): \\`~/.claude/hooks/sync-hook-scripts.sh\\` reads the manifest, does per-entry diff + copy. Keeps sync.md concise.\n\n3. **New sync step**: for each entry, diff master vs mirror; if different, copy master → mirror; SKIP if mirror's containing repo has uncommitted changes the user hasn't addressed (honor sync's \"don't silently discard\" rule); report count in summary.\n\n4. **Scope exclusions** (document inline): manifest must NOT include \\`conductor-mcp\\` (per cm-9vs) or \\`pocketforge\\` (self-contained per cm-7jr).\n\n### Files\n- \\`~/.claude/commands/sync.md\\` — add the new step\n- \\`~/.claude/hooks/sync-manifest.json\\` — new\n- \\`~/.claude/hooks/sync-hook-scripts.sh\\` — new (optional but cleaner than inline bash in markdown)\n\n### Verify\n- Edit master; run \\`/sync\\`; \\`audio-alerts\\` mirror shows same bytes\n- \\`/sync\\` with uncommitted work in mirror repo → skip + report, no silent clobber\n- Pocketforge and conductor-mcp untouched\n\n### When Done\n\\`bd close cm-dl4 --reason 'propagation step added; manifest covers audio-alerts'\\`\n## Retro\n- What worked: Task brief named concrete paths and exact manifest shape; jq + TSV keeps the helper tiny; one-command verification (byte-cmp plus dirty-mirror simulation) surfaced the `${p#~/}` tilde-expansion bug on the first real run. Idempotent second run confirmed the in-sync short-circuit.\n- What was unclear: The brief said the master was 509 lines, but on this device `~/.claude/hooks/scripts/state-tracker.sh` is actually a symlink into `~/projects/therminal/resources/hooks/` (the real authoring copy). The helper handles it via `readlink -f`, and the 'master' label in the manifest still points at the Claude-config-facing path, which is what downstream hooks see — but a future maintainer may want that symlink documented somewhere more visible than this note.\n- Gotcha: bash `${p#~/}` expands `~` in the *pattern*, so it strips `/home/marci/` instead of the literal `~/` prefix. Use `${p#\\\\~/}` to force a literal match. Caught at first run by the `MISSING master: /home/marci/~/.claude/...` error.","status":"closed","priority":2,"issue_type":"feature","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-23T15:33:02Z","created_by":"GGPrompts","updated_at":"2026-04-23T15:49:42Z","closed_at":"2026-04-23T15:49:42Z","close_reason":"propagation step added; manifest covers audio-alerts","labels":["devex","hooks","ready","sync"],"dependencies":[{"issue_id":"cm-dl4","depends_on_id":"cm-7jr","type":"related","created_at":"2026-04-23T11:33:05Z","created_by":"GGPrompts","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"cm-2k1","title":"Audio-config bridge: canonical config ↔ audio-config.sh","description":"## Why\n\nHook scripts (`~/.claude/hooks/scripts/audio-announcer.sh` and consumers of `audio-config.sh`) must keep working when the conductor MCP server is not attached — they fire in every Claude Code session regardless. When the TUI or MCP writes audio settings to the canonical config, the hook-readable form must stay in sync.\n\n## Scope\n\nImplement the strategy chosen in the format-decision issue:\n\n**If `jq`-direct (a):**\n- Update `audio-announcer.sh` (and any other hook consumer) to `jq` the canonical file directly\n- Document `jq` as a required dependency\n- Retire `audio-config.sh` (or keep as a fallback when jq is missing)\n\n**If regenerated shim (b):**\n- TUI + MCP server both call a small `write_audio_shim()` routine whenever audio settings change\n- Shim writes `~/.claude/audio-config.sh` in the current env-var format\n- Add a header comment: \"GENERATED — do not edit. Run TUI settings to change.\"\n- Guarantee idempotent regeneration\n\n## Acceptance\n\n- `/audio` slash command still works end-to-end\n- Hook fires correctly when MCP is not running\n- Settings edited in TUI take effect in next hook fire without Claude Code restart","notes":"## prepared.prompt\n\n### Context\nAudio settings must be readable by hook scripts when the MCP server is NOT running — hooks fire in every Claude Code session regardless. cm-d64 decides the canonical config location and whether hooks read it via \\`jq\\` directly (option a) or via a regenerated \\`.sh\\` shim (option b). This issue implements whichever path cm-d64 chose.\n\n### Task\nImplement per cm-d64 decision.\n\n**If jq-direct (a):**\n- Update \\`~/.claude/hooks/scripts/audio-announcer.sh\\` to read audio fields from the canonical config via \\`jq\\`\n- Document \\`jq\\` as required dependency in root \\`CLAUDE.md\\`\n- Decide fate of \\`~/.claude/audio-config.sh\\` (delete, or keep as fallback for missing jq)\n\n**If regenerated shim (b):**\n- Add \\`write_audio_shim()\\` routine callable from MCP server (Python) and TUI (Go)\n- Shim writes \\`~/.claude/audio-config.sh\\` in current env-var format with header: \\`# GENERATED — do not edit, run TUI settings or /audio\\`\n- Call the routine whenever audio settings change (TUI Settings tab save path + MCP \\`set_config\\`)\n- Make idempotent: regenerating shim with unchanged settings produces byte-identical output\n\n### Files\n- \\`~/.claude/hooks/scripts/audio-announcer.sh\\`\n- \\`~/.claude/audio-config.sh\\` (may become generated)\n- Canonical config writers (server.py in conductor-mcp, tui/config.go + tui/update_keyboard.go)\n- Root \\`CLAUDE.md\\` (dependency note if jq-direct)\n\n### Verify\n- \\`/audio\\` slash command still works end-to-end (list + change + test)\n- Audio hook fires correctly when MCP server is NOT running (disable MCP, change audio, trigger an event)\n- TUI Settings → audio change → next event's hook uses new settings without Claude Code restart\n\n### When Done\n\\`bd close cm-2k1 --reason 'audio bridge: \u003cjq|shim\u003e strategy implemented'\\`\n## Retro\n- What worked: jq-direct read from canonical config with explicit null-check (not `// empty`) correctly preserves literal `false` and `0` values for toggle/numeric fields. Fallback to `~/.claude/audio-config.sh` kept script drop-in compatible for devices without canonical config.\n- What was unclear: initial `// empty` pattern silently dropped `announce_tools: false` — caught during verification. Also `set -euo pipefail` combined with trailing `[[ -n \"$v\" ]] \u0026\u0026 export` in `load_audio_config` made the function inherit exit=1 when the last field (custom_clips_dir) was null, aborting the whole script; fixed with explicit `return 0`. Two subtle bash/jq gotchas worth documenting for future bridge scripts.","status":"closed","priority":2,"issue_type":"task","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-23T15:18:31Z","created_by":"GGPrompts","updated_at":"2026-04-23T15:55:08Z","closed_at":"2026-04-23T15:55:08Z","close_reason":"audio bridge: jq-direct strategy implemented; audio-config.sh kept as fallback","labels":["audio","config","docs:update","hooks","ready"],"dependencies":[{"issue_id":"cm-2k1","depends_on_id":"cm-7jr","type":"blocks","created_at":"2026-04-23T11:18:45Z","created_by":"GGPrompts","metadata":"{}"},{"issue_id":"cm-2k1","depends_on_id":"cm-d64","type":"blocks","created_at":"2026-04-23T11:18:44Z","created_by":"GGPrompts","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"id":"cm-2cx","title":"Fix agent view JSONL rendering for Claude Code conversations","description":"The agent view (🤖 button / Ctrl+A) browses ~/.claude/projects/{hash}/ but has several issues:\n- Shows AI files that aren't actual conversations\n- JSONL rendering doesn't handle the full Claude Code message format well\n- No live tailing for active sessions\n- Doesn't know about subagent JSONL paths\n\n## Reference implementation\n~/projects/thermal-desktop has a Rust-based JSONL tailer and renderer that handles all of this well:\n- `crates/thermal-conductor/src/window/jsonl_tailer.rs` — incremental byte-offset tailing\n- `crates/thermal-conductor/src/viewer.rs` — renders SessionEvents with color palette, auto-expands final report\n- `crates/thermal-conductor/src/session_log.rs` — full JSONL format parsing\n\n## What needs to change\n\n### 1. Filter non-conversation files (agent_awareness.go)\n- Only show .jsonl files that contain actual conversation messages (user/assistant types)\n- Skip config files, metadata, etc. that happen to be JSONL\n\n### 2. Live tailing for active sessions (render_jsonl.go)\n- Currently loads file once on preview open\n- Need periodic refresh using incremental byte-offset reads (like thermal-desktop's SessionJsonlTailer)\n- Auto-scroll to bottom when following an active session (follow mode)\n\n### 3. Subagent JSONL support (agent_awareness.go)\n- Subagent conversations live at: ~/.claude/projects/{hash}/{parent_session}/subagents/agent-{id}.jsonl\n- Show these nested under parent sessions in agent view\n- thermal-desktop detects them via non-null parent_session_id in state files\n\n### 4. Better JSONL rendering (render_jsonl.go)\n- Current renderer handles basic message types\n- Needs to match thermal-desktop's handling of: thinking blocks, tool_use with args, tool_result with output, progress events\n- Collapse long tool outputs, expand final assistant message\n\n## Integration with conductor-mcp\nOnce fixed, conductor-mcp can spawn `tfe --preview \u003cjsonl-path\u003e` in a split pane to monitor any session's conversation live. No rendering code needed in conductor itself.","status":"closed","priority":2,"issue_type":"feature","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-05T01:38:31Z","created_by":"GGPrompts","updated_at":"2026-04-05T01:40:54Z","closed_at":"2026-04-05T01:40:54Z","close_reason":"Completed","labels":["agent-view","jsonl","live-tailing"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"cm-f8t","title":"Add Beads tab to conductor-tui with issue browsing and worker spawning","description":"Add a Beads tab alongside Sessions and Templates in conductor-tui for browsing and acting on issues.\n\n## List view (Beads tab)\n- Run `bd list` or `bd ready` to populate\n- Show each issue as: `[priority] id — title | short description`\n- Color-code by status (open=green, in_progress=yellow, blocked=red)\n- Filter toggles: ready only, by label, by status\n- Stats summary in footer (open/in_progress/blocked/closed counts)\n\n## Detail view (Enter on an issue)\n- Suspend the TUI and run `bd show \u003cissue_id\u003e` in the terminal\n- User sees full details (deps, notes, acceptance criteria, etc.)\n- Press q/Esc or wait for bd show to exit, TUI resumes\n- Same pattern as how the TUI already handles session attach\n\n## Quick actions\n- `Enter` — bd show (full details)\n- `s` — spawn a conductor worker for this issue (smart_spawn with issue context)\n- `c` — claim the issue (bd claim)\n- `x` — close the issue (bd close)\n- `r` — refresh the list\n- `/` — search/filter issues\n\n## Implementation\n- Shell out to `bd` CLI (not MCP) since the TUI is a Go binary with direct shell access\n- Parse `bd list --json` or `bd ready --json` for structured data\n- Config: which project prefix to show, default filters\n- Detect `.beads/config.yaml` in default_dir to auto-scope","notes":"Split the list into two views: an active table (open, in_progress, blocked) shown by default, and a separate Done table toggled with `d` or a tab. The active table stays compact and actionable. Done table can be sorted by closed date, most recent first.","status":"closed","priority":2,"issue_type":"feature","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-05T01:20:40Z","created_by":"GGPrompts","updated_at":"2026-04-05T01:26:04Z","closed_at":"2026-04-05T01:26:04Z","close_reason":"Completed","labels":["beads","integration","tui"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"cm-3gw","title":"Move user-facing settings from MCP tools to conductor-tui settings menu","description":"Several conductor MCP tools exist only for user-facing config that Claude doesn't need to reason about. These burn system prompt tokens (tool definitions) and add round-trip latency (user → Claude → MCP → config file) for simple preference changes. Move them to a Settings tab/menu in conductor-tui where the user can adjust them directly.\n\n## MCP tools to move (remove from server.py after TUI handles them)\n\n### Voice/Audio Settings\n- `list_voices()` — browse available TTS voices and see assignments\n- `test_voice(voice, text)` — preview a voice\n- `reset_voice_assignments()` — clear all worker voice assignments\n- `set_config(default_voice=, voice_rate=, voice_pitch=, random_voices=)` — voice params\n\n### Profile Management\n- `add_profile(name, command, dir)` — create/update spawn profiles\n- `remove_profile(name)` — delete spawn profiles\n- `list_profiles()` — browse profiles with resolved dirs\n\n### General Preferences\n- `set_config(default_layout=, default_dir=, send_keys_delay_ms=, claude_boot_delay_s=)` — layout/timing tweaks\n- `get_config()` — view current config (read-only, useful for Claude too — may keep)\n\n## MCP tools to KEEP (Claude needs these for orchestration)\n- `spawn_worker`, `smart_spawn`, `smart_spawn_wave` — need profiles resolved at spawn time\n- `speak` — Claude triggers TTS programmatically\n- `send_keys`, `kill_worker`, `list_workers` — core orchestration\n- `get_worker_status`, `get_context_percent`, `get_workers_with_capacity` — decision-making\n- All pane/session/hook/signal/watch tools — orchestration primitives\n- `get_config()` — Claude may still need to read config for spawn decisions\n\n## Skill to remove/simplify\n- `conductor:help` skill's Settings and Profiles sections would become \"open conductor-tui\" instead of the current AskUserQuestion → MCP call flow\n\n## TUI implementation\n- Add a Settings tab or panel in conductor-tui (alongside Sessions and Templates)\n- Read/write `~/.config/conductor-mcp/config.json` directly (same file MCP uses)\n- Sections: Voice (picker + test + rate/pitch), Profiles (CRUD table), Layout/Timing\n- Voice test can shell out to edge-tts + mpv directly","notes":"## Coordination with audio-config\n\nThe original description assumed \\`~/.config/conductor-mcp/config.json\\` is the single source. That's incomplete — audio settings currently live in \\`~/.claude/audio-config.sh\\`, which is sourced by hook scripts that run WITHOUT the MCP server attached. The settings tab design must account for this.\n\nSee cm-d64 (format + shim strategy decision) — pick canonical file path/format and whether hooks read it via \\`jq\\` or via a regenerated \\`.sh\\` shim. That decision governs whether this issue's \"read/write ~/.config/conductor-mcp/config.json\" line is correct or needs to be widened to a new canonical config path. This issue is now blocked-by cm-d64 (was related).\n\nRelated: cm-2k1 (audio bridge) shares the config decision. cm-9vs (remove plugin hook duplication) — separate hook-ownership cleanup.\n\n## prepared.prompt\n\n### Context\nSeveral conductor MCP tools exist only for user-facing config (voice params, profile CRUD, layout/timing tweaks). They burn system prompt tokens and add round-trip latency. Move them to a Settings tab in conductor-tui. The canonical config path/format comes from cm-d64.\n\n### Task\n1. **Add Settings panel/tab** in conductor-tui alongside Sessions/Templates — match existing UX (likely a tab within panel 1, or panel 4)\n2. **Sections**: Voice (picker + test button + rate/pitch sliders), Profiles (CRUD table), Layout/Timing toggles\n3. **Read/write canonical config** file chosen in cm-d64\n4. **Voice test**: shell out to edge-tts + mpv directly — do not require MCP server\n5. **Remove MCP tools** from \\`server.py\\` (after TUI covers them): \\`list_voices\\`, \\`test_voice\\`, \\`reset_voice_assignments\\`, \\`add_profile\\`, \\`remove_profile\\`, \\`list_profiles\\`, user-facing params of \\`set_config\\`. **Keep** \\`get_config\\` (Claude may still need it for spawn decisions).\n6. **Simplify skill**: update \\`conductor:help\\` / \\`conductor:options\\` to point users at the TUI for settings/profiles rather than the current AskUserQuestion → MCP-tool flow.\n\n### Files\n- \\`tui/view.go\\`, \\`tui/update_keyboard.go\\`, \\`tui/styles.go\\` — new panel/tab rendering + input\n- \\`tui/config.go\\` — canonical config read/write (per cm-d64)\n- \\`tui/tmux.go\\` or new \\`tui/audio.go\\` — voice test (edge-tts + mpv)\n- \\`server.py\\` — remove migrated MCP tools\n- \\`plugins/conductor/skills/conductor/SKILL.md\\` (and marketplace copy) — update help skill\n\n### Verify\n- \\`Ctrl+b o\\` → Settings → change voice → sample plays immediately\n- Config change persists across Claude Code restart\n- MCP server no longer exposes removed tools (\\`discover_tools\\`)\n- \\`conductor:help\\` skill no longer references removed MCP tools\n- TUI rebuild clean (\\`cd tui \u0026\u0026 go build -o conductor-tui .\\`)\n\n### When Done\n\\`bd close cm-3gw --reason 'settings migrated to TUI; \u003cN\u003e MCP tools removed'\\` — include the actual count.\n\n## Retro\n\n### What worked\n- Canonical config migration path (cm-d64 schema) slotted in cleanly: server.py's existing load_config already had a defaults-merge loop, so adding a first-run migrator was a small localized change.\n- tui/config.go switch from YAML to JSON required rewriting read/write + a generic root-map helper. The Settings panel reuses that root map for voice/profiles/timing without disturbing the TUI's typed Config struct.\n- Adding the Settings tab alongside Sessions/Templates reused all existing panel/focus/scroll plumbing. Only view.go's title/focus/scroll-offset tuples needed updating plus a tab-cycle branch in update_keyboard.go.\n- Voice test shells out to edge-tts + mpv using the same invocation pattern as server.py's speak(), so preview works with no MCP dependency.\n- saveConfig preserves unknown top-level sections so the TUI can't stomp MCP-owned state (profiles, voices runtime, audio settings).\n\n### What was unclear\n- cm-d64's schema uses 'audio' for voice settings but server.py's current config still uses 'voice' top-level. TUI reads both voice.default OR audio.default_voice and writes 'voice.default' to match the current MCP schema. cm-2k1 may migrate to 'audio' fully — TUI needs a one-line update at that point.\n- Profile CRUD and Timing editing are stubbed in the Settings panel (coming soon). Shipping Voice as the MVP per the task's fallback plan — full CRUD for Profiles would require modal input state.\n- testVoice uses a fixed tmp path; rapid clicks can overwrite but it's harmless.\n\n### Scope discipline\n- Root CLAUDE.md Requirements section intentionally untouched (cm-2k1 owns that).\n- audio-announcer.sh and audio-config.sh intentionally untouched (cm-2k1 owns that).\n\n### Follow-up\n- File a follow-up issue for Profiles CRUD modal + Timing value-editor in Settings panel.","status":"closed","priority":2,"issue_type":"feature","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-05T01:10:01Z","created_by":"GGPrompts","updated_at":"2026-04-23T16:03:31Z","closed_at":"2026-04-23T16:03:31Z","close_reason":"settings migrated to TUI; 7 MCP tools removed (list_voices, test_voice, reset_voice_assignments, add_profile, remove_profile, list_profiles, set_config); canonical config at ~/.config/conductor/config.json; Voice section shipped as MVP, Profiles+Timing editors deferred to cm-b6r","labels":["docs:update","ready","settings","token-optimization","tui"],"dependencies":[{"issue_id":"cm-3gw","depends_on_id":"cm-d64","type":"blocks","created_at":"2026-04-23T11:36:46Z","created_by":"GGPrompts","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"id":"cm-yk2","title":"Integrate tmuxplexer TUI into conductor-mcp","description":"Port the tmuxplexer Go TUI (Bubble Tea) into conductor-mcp as a companion binary. Tmuxplexer already reads the same `/tmp/claude-code-state/*.json` files that conductor's hooks write, making them natural companions.\n\n## What to copy from ~/projects/tmuxplexer\n\n**Go TUI source files** (into `tui/` directory):\n- `main.go` — entry point, `--popup` flag, TTY detection\n- `types.go` — Model struct, config types, session/template types\n- `model.go` — initialization, layout calculations, session loading\n- `view.go` — 3-panel rendering (sessions, preview, command)\n- `update.go`, `update_keyboard.go`, `update_mouse.go` — input handling\n- `styles.go` — lipgloss styling\n- `config.go` — YAML config loading (~/.config/tmuxplexer/)\n- `templates.go` — template CRUD\n- `tmux.go` — tmux command wrappers\n- `claude_state.go` — state file reading, status formatting, context % display\n\n**NOT needed** (conductor already has better versions):\n- `hooks/state-tracker.sh` — conductor's version has subagent counting + context linking\n- `hooks/audio-announcer.sh` — conductor's is equivalent and already wired up\n\n## Goals\n\n1. Provide a human-facing TUI dashboard for conductor workers (Ctrl+b o popup)\n2. Move user-facing settings (voice/audio config, profiles, keybindings) into the TUI — reducing system prompt token usage by removing skills Claude doesn't need to reason about\n3. Session list with live Claude status, attach, kill, send commands\n4. Keep tmuxplexer as-is as a backup — this is a copy + adapt, not a move\n\n## Integration points\n\n- TUI reads conductor's `~/.config/conductor-mcp/config.json` for profiles/voice settings\n- TUI reads same `/tmp/claude-code-state/*.json` state files the hooks already write\n- Install script sets up `Ctrl+b o` tmux binding pointing to the conductor TUI binary\n- Conductor's `show_popup`/`show_status_popup` could optionally delegate to the TUI when available","notes":"Ported all 14 Go source files + go.mod/go.sum to tui/ directory. Renamed module to conductor-tui, replaced all tmuxplexer references (config paths, binary name, help text). Builds clean — 5.5MB binary. Ready for customization.","status":"in_progress","priority":2,"issue_type":"feature","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-05T00:47:12Z","created_by":"GGPrompts","updated_at":"2026-04-05T01:00:17Z","labels":["integration","tui"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"cm-xbf","title":"Explore Claude Code native worktree + cwd hooks","description":"Claude Code now exposes `WorktreeCreate`, `WorktreeRemove`, `CwdChanged`, and `FileChanged` hook events (visible in /hooks dialog). Evaluate whether conductor should wire any of these in.\n\n**Current state:** conductor's `spawn_worker` runs `git worktree add` directly and handles TTS/context injection inline. No CC hooks involved.\n\n**Findings from prior exploration:**\n- CC `WorktreeCreate/Remove` hooks only fire for CC's own worktree ops (Agent(isolation=\"worktree\"), EnterWorktree). They do NOT fire for `git worktree add` run via subprocess — so conductor's spawn_worker wouldn't trigger them today.\n- `bd worktree create/remove` is just a wrapper over `git worktree` with safety checks on remove. Any git worktree already shares the beads DB automatically via git common directory discovery — bd isn't needed for DB sharing.\n- User constraint: conductor must remain usable without beads installed. So `bd worktree` is off the table as a hard dependency.\n\n**What to decide:**\n1. Should conductor adopt CC's native Agent(isolation=\"worktree\") for any spawn flow? (different shape — ephemeral subagent, not interactive tmux pane — probably no)\n2. Is there value in firing conductor TTS announcements from WorktreeCreate/Remove hooks instead of inline? (would cover manual `git worktree add` too)\n3. `CwdChanged` + auto `bd context` on entering a beads-enabled project — convenient but leaks beads coupling into the global hook layer. Defer unless there's a concrete pain point.\n4. `FileChanged` watching `~/.config/conductor/config.json` for hot-reload on voice/profile edits — interesting but low priority; TUI already handles explicit saves.","notes":"## prepared.prompt\n\n## Task\nExploration only — no code changes expected. Write findings as notes on this issue and close.\n\nFor each of the CC hook events (WorktreeCreate, WorktreeRemove, CwdChanged, FileChanged):\n1. Does conductor-mcp have a concrete use case today?\n2. Would adopting it introduce coupling (e.g., forcing beads CLI for all users)?\n3. If valuable, what's the smallest follow-up issue to file?\n\n## Context\nExisting investigation summary is in the description. Constraint: conductor must remain usable without beads installed. Build a recommendation: adopt / defer / reject, per event.\n\n## When Done\nbd update cm-xbf --append-notes \"## Findings\\n...\"\nbd close cm-xbf --reason \"exploration complete — decisions recorded in notes\"\n## Findings (cm-xbf exploration)\n\n### WorktreeCreate / WorktreeRemove\n- **Use case today**: NO concrete use case. conductor's `spawn_worker()` calls `git worktree add` via subprocess (line 414–424), which does NOT trigger CC's WorktreeCreate hook. The hook only fires for CC's own Agent(isolation=\"worktree\") or EnterWorktree tool — not raw git commands.\n- **Coupling risk**: MEDIUM. If adopted, conductor would need to either (a) switch to CC's EnterWorktree tool (fundamentally different shape — ephemeral subagent, not interactive tmux pane) or (b) fire the hook manually from Python (redundant with git's own mechanisms). Could force non-beads users to have CC hooks configured just to use conductor.\n- **Recommendation**: DEFER. No pain point today. The current pattern (subprocess git worktree + optional beads context injection) works well and stays beads-optional. Revisit only if manual `git worktree add` workflows need TTS announcements or other cross-tool coordination.\n- **Follow-up**: None\n\n### CwdChanged\n- **Use case today**: NO concrete use case. conductor spawns workers in worktree-specific directories but does not rely on CwdChanged to coordinate state.\n- **Coupling risk**: HIGH. Auto-running `bd context` on CwdChanged would leak beads as a global dependency — every user entering any project would trigger beads discovery, even if they're not using beads. Breaks conductor's principle of remaining usable without beads installed.\n- **Recommendation**: REJECT. The coupling cost outweighs the convenience. Users who want auto-context injection should use beads's own CLI or configure it in their shell profile, not via conductor's hook layer.\n- **Follow-up**: None\n\n### FileChanged\n- **Use case today**: WEAK but interesting. conductor-tui already handles explicit config saves with manual reloads (via Settings panel). Hot-reload on edits to `~/.config/conductor/config.json` would be nice for power users editing voice/profile settings, but it's not urgent — workers running in parallel rarely conflict on config (each loads once at spawn).\n- **Coupling risk**: LOW. Watching a single file path is safe and doesn't force beads. However, the current reload pattern is already working; adding FileChanged would add complexity (watch loop + debounce) for minimal UX gain.\n- **Recommendation**: DEFER. Nice-to-have, but not worth the complexity cost. conductor-tui Settings panel + manual config edits are sufficient. If a future use case emerges (e.g., dynamic voice switching across many running workers), revisit then.\n- **Follow-up**: None\n\n### Decision summary\nAll three CC hook events have either no concrete use case (Worktree*, CwdChanged) or low urgency (FileChanged). The coupling risks for Worktree and CwdChanged outweigh benefits given the beads-optional constraint. Stick with current architecture: subprocess-based worktree creation, try-except beads context injection, and explicit config management.","status":"closed","priority":3,"issue_type":"task","owner":"marcinek.matthew@gmail.com","created_at":"2026-04-23T16:40:47Z","created_by":"GGPrompts","updated_at":"2026-04-23T17:58:14Z","closed_at":"2026-04-23T17:58:14Z","close_reason":"exploration complete — decisions recorded in notes","labels":["exploration","hooks","ready"],"dependency_count":0,"dependent_count":0,"comment_count":0}
